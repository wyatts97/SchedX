# ============================================
# SchedX Production Environment Configuration
# ============================================
# SECURITY WARNING: Never commit this file with real values!
# Use environment variables or secrets management in production

# ============================================
# AUTHENTICATION & SECURITY
# ============================================

# CRITICAL: Generate strong, unique secrets for production
# Use: openssl rand -base64 48 for extra security
AUTH_SECRET=REPLACE_WITH_STRONG_PRODUCTION_SECRET_MIN_48_CHARS

# CRITICAL: Use different encryption key than development
DB_ENCRYPTION_KEY=REPLACE_WITH_STRONG_PRODUCTION_ENCRYPTION_KEY_MIN_48_CHARS

# ============================================
# DATABASE
# ============================================

# Production MongoDB connection (use managed service recommended)
# Examples:
#   MongoDB Atlas: mongodb+srv://user:pass@cluster.mongodb.net/schedx?retryWrites=true&w=majority
#   Self-hosted: mongodb://user:pass@mongodb.internal:27017/schedx?authSource=admin
MONGODB_URI=mongodb+srv://YOUR_USER:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/schedx

# ============================================
# SERVER CONFIGURATION
# ============================================

# Bind to all interfaces in production
HOST=0.0.0.0

# CRITICAL: Set to your production domain with HTTPS
# Must match Twitter OAuth callback URL
ORIGIN=https://yourdomain.com

# Production port (often 3000, 8080, or behind reverse proxy)
PORT=3000

# CRITICAL: Must be set to production
NODE_ENV=production

# ============================================
# FILE UPLOADS
# ============================================

# Stricter limits for production
MAX_UPLOAD_SIZE=10485760
BODY_SIZE_LIMIT=10485760

# ============================================
# SCHEDULER CONFIGURATION
# ============================================

# More frequent checks in production (every minute)
CRON_SCHEDULE=*/1 * * * *

# ============================================
# LOGGING & MONITORING
# ============================================

# Production log level (info or warn recommended)
LOG_LEVEL=info

# REQUIRED: Sentry DSN for production error tracking
# Get from: https://sentry.io/settings/projects/
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Production environment identifier
SENTRY_ENVIRONMENT=production

# ============================================
# PRODUCTION SETTINGS
# ============================================

# Disable debug mode in production
DEBUG=false

# Shorter session timeout for security
SESSION_TIMEOUT=14400

# Account limits
MAX_ACCOUNTS_PER_USER=10

# Stricter rate limiting
RATE_LIMIT_PER_MINUTE=60

# ============================================
# DOCKER PRODUCTION SETTINGS
# ============================================

# MongoDB credentials for Docker Compose
MONGO_USER=schedx_admin
MONGO_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
MONGO_DATABASE=schedx

# Email Notifications
EMAIL_NOTIFICATIONS_ENABLED=true
EMAIL_PROVIDER=resend
RESEND_API_KEY=yourkeyhere
EMAIL_FROM=onboarding@resend.dev
EMAIL_FROM_NAME=SchedX